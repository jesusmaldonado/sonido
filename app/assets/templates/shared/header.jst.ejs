  <div class="navlinks">
    <ul class="sidebarlinks">
      <li class="sidebarlink">
        <a href="" > Home </a>
      </li>
      <li class="sidebarlink">
        <a href="#myplaylists"> Playlists </a>
      </li>
      <% if (currentUser.escape('account_type') === "artist") {%>
      <li class="sidebarlink">
        <a href="#myuploads"> Uploads </a>
      </li>
      <% }  %>
      <li class="sidebarlink">
        <a href="#mylikes"> Likes </a>
      </li>
      <li>
        <a href="#mysettings"> Settings </a>
      </li>
    </ul>
  </div>

<nav class="navbar">
    <ul class="userinfo">
    <% if (Sonido.currentUser.isSignedIn()) { %>
        <li><%= currentUser.escape('username') %></li>
        <li>
          <div class="profile-picture">
            <img src="<%= currentUser.escape('avatar_url') %>">
          </div>
        <li>
          <button class="logout">
            Log Out
          </button>
        </li>

        <% if (currentUser.escape('account_type') === "artist") { %>

          <li>
            <a href="#/newRecording">
              <button>
                Release a New Album, EP or Single
              </button>
            </a>
          </li>
          <li>
            <a href="#/uploads">
              <button href=>
                View Your Uploads
              </button>
            </a>
          </li>

        <% } %>
    <%  } else { %>

    <a href="#users/new">Sign Up</a> |
    <a href="#session/new">Sign In</a>
    <% } %>
    </ul>

</nav>
